NODE_ENV=production
PORT=3000
DB_HOST=mariadb
DB_PORT=3306
DB_NAME=pixel
DB_USER=pixel
DB_PASSWORD=
# 폴링 주기 (초) — 기록 정확도와 무관, 홈 화면 신선도만 영향
POLL_INTERVAL_SEC=60
# 관리자 페이지 인증 키 (openssl rand -hex 24)
# 관리자 로그인 (아이디/비밀번호). 로그인 성공 시 ADMIN_KEY를 토큰으로 발급
# JWT 서명 시크릿 (openssl rand -hex 32)
JWT_SECRET=
